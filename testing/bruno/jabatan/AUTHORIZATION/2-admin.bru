meta {
  name: 2. Role Admin
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/jabatan.php
  body: none
  auth: none
}

tests {
  test("Akses diterima (Asumsi sudah login sebagai admin)", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

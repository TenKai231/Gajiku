meta {
  name: 1. Admin Execute Generate
  type: http
  seq: 1
}
post {
  url: http://localhost:8080/?action=payroll/process
  body: formUrlEncoded
  auth: none
}
body:form-urlencoded {
  bulan: 09
  tahun: 2026
}

tests {
  test("Admin boleh generate gaji", function() {
    // ADMIN: payroll/process.php → success → 302 redirect ke ?page=payroll/proses&bulan&tahun
    // Bruno follow-redirect default → final status 200 dari halaman proses.
    expect(res.getStatus()).to.be.oneOf([200, 302]);
  });
}

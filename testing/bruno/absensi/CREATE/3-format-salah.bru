meta {
  name: 3. Create Format Salah
  type: http
  seq: 3
}
post {
  url: http://localhost:8080/absensi.php?action=create
  body: formUrlEncoded
  auth: none
}
body:form-urlencoded {
  karyawan_id: abc
  tanggal: bukan-tanggal
  status_absensi: Libur
}
tests {
  test("Validasi format gagal", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

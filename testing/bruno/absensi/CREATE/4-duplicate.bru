meta {
  name: 4. Create Duplicate (Tgl & Karyawan Sama)
  type: http
  seq: 4
}
post {
  url: http://localhost:8080/absensi.php?action=create
  body: formUrlEncoded
  auth: none
}
body:form-urlencoded {
  karyawan_id: 1
  tanggal: 2026-08-31
  status_absensi: Sakit
}
tests {
  test("Validasi constraint mencegah duplicate absen harian", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

meta {
  name: 2. Read Single (NIP/ID Tidak Tersedia)
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/?page=karyawan/index
  body: none
  auth: none
}

tests {
  test("Mendapatkan respons handling error saat ID tidak ditemukan", function() {
    expect(res.getStatus()).to.be.oneOf([200, 302, 404]);
  });
}
